<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Inherit Form View to Modify it -->
    <record id="view_company_inherit_lm_default_wr_form" model="ir.ui.view">
        <field name="name">res.company.inherit.lm.default.wr.form</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='general_info']" position="after">
                <page name="lm_warehouse" string="Warehouse Configuration">
                    <group>
                        <group>
                            <field name="is_wr_so"/>
                            <field name="is_fc_so"/>
                        </group>
                        <group>
                            <field name="default_wr" attrs="{'invisible':[('is_wr_so', '=' , False)],'required':[('is_wr_so', '!=' , False)]}"/>
                            <field name="default_fc" attrs="{'invisible':[('is_fc_so', '=' , False)],'required':[('is_fc_so', '!=' , False)]}"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>